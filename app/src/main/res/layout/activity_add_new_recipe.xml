<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.AddNewRecipeActivity">

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/add_recipe_tab_layout"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:background="@color/red"
            app:tabTextColor="@color/white"
            app:tabIndicatorColor="@color/white"
            app:tabSelectedTextColor="@color/white">
<!--
                <com.google.android.material.tabs.TabItem
                    android:id="@+id/add_tab_recipe"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/add_tab_recipe" />

                <com.google.android.material.tabs.TabItem
                    android:id="@+id/add_tab_ingredients"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/add_tab_ingredient"/>

                <com.google.android.material.tabs.TabItem
                    android:id="@+id/add_tab_steps"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/add_tab_step"/>

 -->
        </com.google.android.material.tabs.TabLayout>

        <androidx.viewpager.widget.ViewPager
            android:id="@+id/add_recipe_viewPager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

</RelativeLayout>